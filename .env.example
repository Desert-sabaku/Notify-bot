# Discord Configuration
DISCORD_BOT_TOKEN=your_discord_bot_token_here
DISCORD_CHANNEL_ID=your_channel_id_here

# Google Calendar API Credentials
# Paste the contents of your credentials.json file here
# Example: {"installed":{"client_id":"...","client_secret":"...","redirect_uris":["http://localhost"]}}
GOOGLE_CREDENTIALS_JSON=

# OAuth2 Token (required after first authentication)
# After running the app for the first time and authenticating via browser,
# the token will be displayed in the console. Copy and paste it here.
# GOOGLE_TOKEN_YAML_BASE64=LS0tCmRlZmF1bHQ6IC4uLg==

# Optional: Timezone offset (default: +09:00 for JST)
# TIMEZONE_OFFSET=+09:00

# Optional: OAuth callback port (default: 8080)
# OAUTH_PORT=8080

# Optional: Credentials and token file paths
# CREDENTIALS_PATH=credentials.json
# TOKEN_PATH=token.yaml
